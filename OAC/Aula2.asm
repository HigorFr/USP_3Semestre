#Aula 2

#O Assembly é a ponte entre nível baixo e a linguagem de máquina (binário)

    #PC = Program Calculator (Guarda a próxima instrução a ser executada)
    #JR = Ir para a linha do Registrador (Ele pula para o que está guardado lá)
    #ADDI = Adição Imediata
    # SUB = Subtração imediata

    #Estrutura IAS é a principal que vimos hoje em dia, e foi evoluindo a partir dela
        #Em resumo, são entradas e saída, o Unidade Aritimetica lógica (ALU), o PRogram Control Unit (PCU) e a memória. Todos conectados
        #No slide tem o nível ainda mais baixo
        #Gargalo de Von Noram (Inventor dessa arquitetura) é no acesso à ram. Ou seja, a CPU evita ao máximo ler a RAM pois a diferença de velocidade deles é imensa
            #Isso é ajustado pelo barramento, ele que faz a gestão dessa diferença de velocidade (Em uma placa mãe tem muitos barramentos diferente)


#OP Code é o que forma o binario inicial da instrução do processador, ele diz quais operações serão feitas  (Se é uma soma, ele vai procurar endereços nos numeros binarios posteriores)


#Consersão HEX/BIN para DEC
    #






#Circuito lógicos